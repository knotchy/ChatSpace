.side-bar__index
  - current_user.groups.each do |group|
    = link_to group_messages_path(group),class: "border_btm" do
      .side-bar__groupboxes
        .group__name
          = group.name
        .group__message{style:'padding-top: 5px;font-size: 11px;'}
          = group.show_last_message
